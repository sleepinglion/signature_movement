<aside id="main-menu" class="col-12 col-md-2 col-xxl-1">
  <h1><a href="/admin"><%= t(:application_name) %></a></h1>
  <nav>
    <ul class="nav">
      <li class="nav-item
            <% if ['admin/reports','admin/report_categories'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to admin_reports_path, :class => "nav-link" do %>
          <i class="material-icons">local_cafe</i><span><%= t(:report, scope: [:activerecord, :models]) %></span>
        <% end %>
      </li>
      <li class="nav-item
            <% if ['admin/compliments','admin/compliment_categories'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to admin_compliments_path, :class => "nav-link" do %>
          <i class="material-icons">local_cafe</i><span><%= t(:compliment, scope: [:activerecord, :models]) %></span>
        <% end %>
      </li>
      <li class="nav-item
            <% if ['admin/notices'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to admin_notices_path, :class => "nav-link" do %>
          <i class="material-icons">local_cafe</i><span><%= t(:notice, scope: [:activerecord, :models]) %></span>
        <% end %>
      </li>
      <li class="nav-item
            <% if ['admin/users'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to admin_users_path, :class => "nav-link" do %>
          <i class="material-icons">person</i><span><%= t(:user, scope: [:activerecord, :models]) %></span>
        <% end %>
      </li>
    </ul>
  </nav>
</aside>
